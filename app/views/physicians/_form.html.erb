<%= form_with(model: physician, local: true) do |form| %>
  <% if physician.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(physician.errors.count, "error") %> prohibited this physician from being saved:</h2>

      <ul>
        <% physician.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Name %>
    <%= form.text_field :Name %>
  </div>

  <div class="field">
    <%= form.label :Specialty %>
    <%= form.text_field :Specialty %>
  </div>

  <h3>Office Locations</h3>
  <div id="office_locations">
    <%= form.fields_for :office_locations do |office_location| %>
      <%= render 'office_location_fields', :f => office_location %>
    <% end %>
    <div class="links">
      <%= link_to_add_association 'Add Office Location', form, :office_locations %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
